SRC="/src"
DEST="/dest"
BACKUP_OPTIONS="--dry-run -a"
RESTORE_OPTIONS="--dry-run -h"

before() {
	#run_as_root
	#check_mountpoint $DEST 
	echo "before"
}

backup() {
	echo "backup"
	brsync root
}

restore() {
	echo "restore"
	rrsync root
}

after() {
	echo "after"
}

# vim: ft=sh
